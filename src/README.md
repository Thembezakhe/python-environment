# Source Directory

This directory contains reusable Python modules and packages.

Modules placed here can be imported and used across notebooks, scripts,
and automation code.

Example usage:
```python
from src.some_module import some_function